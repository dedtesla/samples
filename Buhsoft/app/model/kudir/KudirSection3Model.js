Ext.define('Buh.model.kudir.KudirSection3Model', {
    extend: 'Ext.data.Model',
    requires: [
        'Ext.data.reader.Json'
    ],
    fields: [
        'id', 'indicator', 'row', 'summa'
    ]
});